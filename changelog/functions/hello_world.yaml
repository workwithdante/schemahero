apiVersion: schemas.schemahero.io/v1alpha4
kind: Function
metadata:
  name: hello-world
  namespace: schemahero
spec:
  database: postgres
  name: hello_world
  schema:
    postgres:
      schema: company
      lang: SQL
      as: |-
        SELECT 'Hello, world!'::text;
      params: []   
